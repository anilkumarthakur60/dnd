{
  "name": "example-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Framework-neutral landing page for @anil-labs/dnd — every demo on it runs the zero-dependency dnd-core engine directly, so it needs no framework runtime.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/check-page.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anil-labs/dnd-core": "workspace:*"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
